// MUI setup for your project
// Chạy lệnh này trong terminal để cài đặt MUI:
// npm install @mui/material @emotion/react @emotion/styled @mui/icons-material

// Sau khi cài xong, bạn có thể import các component MUI như sau:
// import { Button, Table, TableBody, TableCell, TableContainer, TableHead, TableRow, Paper, Drawer, List, ListItem, ListItemIcon, ListItemText, Typography, Box, Avatar, Badge, IconButton } from '@mui/material';
// import { Add, ShoppingCart, People, Settings, Fastfood } from '@mui/icons-material';

// Để sử dụng theme, bạn có thể import ThemeProvider từ @mui/material/styles
// import { ThemeProvider, createTheme } from '@mui/material/styles';

// Bạn có thể bắt đầu refactor Dashboard.jsx và các page theo MUI theo mẫu UI đã gửi.
